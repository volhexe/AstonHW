server:
  port: 8081
spring:
  mail:
    host: smtp.gmail.com  # test
    port: 587
    username: your-email@gmail.com
    password: your-app-password  # test
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: notification-group
      topic: user-events
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka